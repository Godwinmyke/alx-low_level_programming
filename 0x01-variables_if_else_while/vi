#include <stdio.h>
/**
 * main - This the main function
 * Return: Always 0
 */
int main(void)
        int a, b;
        
        for (a = 0 ; a < 9 ; a++)
        {
        for (b = a + 1 ; b < 10 ; b++)
        {       i
        putchar(a + '0');
        putchar(b + '0');
